# Graphify — Codebase Knowledge Graph

Map the PluggedIN codebase into a queryable knowledge graph.

**Action:** Read `skills/graphify/SKILL.md` and follow all steps to generate `graph.json` and `GRAPH_REPORT.md` showing how every lib/, agent, skill, and memory file connects.

**Triggers for:** "map the codebase", "knowledge graph", "what does X connect to", "dependency map", "understand the repo", "graph the project"
